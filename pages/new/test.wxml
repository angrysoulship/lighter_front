<!--pages/new/test.wxml-->
<view class="mainView">
  <view class="bodyView">
    <text class="centerTitle">你现在是什么心情呢？</text>
    <!-- 重点EmojiScrollView -->
    <scroll-view scroll-x="true" scroll-with-animation="true" class="scrollView">
      <view wx:for="{{emojis}}" class="emojiScroll" wx:key="id" >
        <image class="emojiImage" src="{{item.url}}" mode="aspectFill" bindtap="tabSelect" data-name-id="{{item}}"></image>
      </view>
    </scroll-view>
    <!-- 结束重点EmojiScrollView -->
    <view class="moodAxies"></view>
    <button bindtap="enterPostDetailPage" class="mini-btn" type="primary" size="mini">提交</button>
  </view>
  
</view>



